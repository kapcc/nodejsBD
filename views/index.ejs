<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Анимация конверта с текстом</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>


<form action="/create" method="POST">
    <input type="text" name="text" placeholder="Введите текст для дива" required>
    <button type="submit">Добавить</button>
  </form>
  
  <h2>Список дивов:</h2>
  <div>
    <% divs.forEach(div => { %>
      <div class="envelope">
        <p><%= div.text %></p>
      </div>
    <% }); %>
  </div>
<script src="script.js"></script>
</body>
</html>
